# threadsafe_zmq Examples

## Quick Start

### Sync

From project root:

```bash
just example  # Runs sync server + client
```

### Async

From project root:

```bash
just example-async  # Runs sync server + client
```